#include <stdio.h>
#include <string.h>
#include <stdbool.h>

#include "calculator.h"
#include "vector.h"

int main (int argc, char** argv) {

    // If invalid amount of command line arguments is passed:
    printf("Invalid amount of command line arguments is given\n");

    // If invalid command line arguments is passed:
    printf("Invalid argv[1] input\n");

    return 0;
}